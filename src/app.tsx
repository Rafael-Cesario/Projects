import { styled } from "styled-components";

const App = () => {
	return <StyledTitle>Agile Quiz</StyledTitle>;
};

const StyledTitle = styled.h1`
	background-color: blue;
	color: #ddd;
`;

export default App;
