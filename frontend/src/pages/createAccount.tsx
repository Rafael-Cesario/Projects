import { AccountForm, CreateAccountForm } from '../components';

const CreateAccount = () => {
	return (
		<AccountForm title={'Criar Conta'}>
			<CreateAccountForm />
		</AccountForm>
	);
};

export default CreateAccount;
